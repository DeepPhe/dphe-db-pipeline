# Tests

Run the test suite from the project root:

```bash
uv run pytest
```

The suite covers the bundled example pipeline, OMOP JSON importer configuration, parser regressions, and storage helper behavior. Test fixtures live in `tests/resources/`.
